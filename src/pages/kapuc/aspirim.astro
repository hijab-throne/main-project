---
import Layout from '../../layouts/Layout.astro';
import Page from '../../page-components/kapuc/KapucEcardinAspirim.jsx';
import WhatsAppWidget from '../../components/WhatsAppWidget.jsx';
import { buildProductPageJsonLd } from '../../lib/productLd.js';

const pathname = '/kapuc/aspirim';
const jsonLd = buildProductPageJsonLd(pathname, [
  { name: 'Kryefaqja', url: '/' },
  { name: 'Underscarf', url: '/kapuc/tub' },
  { name: 'Ninxha Climatic', url: pathname },
]);
---
<Layout ogType="product" jsonLd={jsonLd}>
  <WhatsAppWidget client:idle />
  <Page client:load pathname={pathname} locale="sq" />
</Layout>
