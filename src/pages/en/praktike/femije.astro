---
import Layout from '../../../layouts/Layout.astro';
import Page from '../../../page-components/praktike/PraktikeFemije.jsx';
import WhatsAppWidget from '../../../components/WhatsAppWidget.jsx';
import { buildProductPageJsonLd } from '../../../lib/productLd.js';

const pathname = '/en/praktike/femije';
const jsonLd = buildProductPageJsonLd(pathname, [
  { name: 'Home', url: '/en' },
  { name: 'Practical', url: '/en/praktike' },
  { name: 'Hijab for Kids', url: pathname },
]);
---
<Layout ogType="product" jsonLd={jsonLd}>
  <WhatsAppWidget client:idle />
  <Page client:load pathname="/praktike/femije" locale="en" />
</Layout>
