---
import Layout from '../../../layouts/Layout.astro';
import Page from '../../../page-components/jersey/Jersey200.jsx';
import WhatsAppWidget from '../../../components/WhatsAppWidget.jsx';
import { buildProductPageJsonLd } from '../../../lib/productLd.js';

const pathname = '/en/jersey/shami-jr-200';
const jsonLd = buildProductPageJsonLd(pathname, [
  { name: 'Home', url: '/en' },
  { name: 'Jersey', url: '/en/jersey' },
  { name: 'Jersey Square 200x100', url: pathname },
]);
---
<Layout ogType="product" jsonLd={jsonLd} locale="en">
  <WhatsAppWidget client:idle />
  <Page client:idle pathname="/jersey/shami-jr-200" locale="en" />
</Layout>
