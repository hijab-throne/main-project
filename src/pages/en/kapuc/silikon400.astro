---
import Layout from '../../../layouts/Layout.astro';
import Page from '../../../page-components/kapuc/KapucSilikon400.jsx';
import WhatsAppWidget from '../../../components/WhatsAppWidget.jsx';
import { buildProductPageJsonLd } from '../../../lib/productLd.js';

const pathname = '/en/kapuc/silikon400';
const jsonLd = buildProductPageJsonLd(pathname, [
  { name: 'Home', url: '/en' },
  { name: 'Underscarf', url: '/en/kapuc' },
  { name: 'Kaymaz Silicone 400', url: pathname },
]);
---
<Layout ogType="product" jsonLd={jsonLd} locale="en">
  <WhatsAppWidget client:idle />
  <Page client:load pathname="/kapuc/silikon400" locale="en" />
</Layout>
